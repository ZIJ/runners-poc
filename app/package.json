{
  "name": "managed-runners-app",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub App webhook service for Managed Runners POC (OpenTofu)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@google-cloud/pubsub": "5.2.0",
    "@octokit/auth-app": "^7.0.0",
    "@octokit/webhooks": "^12.0.0",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
